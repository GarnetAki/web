function clearAll(e) {
    // var buttons = document.getElementsByClassName("setup-zone");

    // for (child in buttons.children) {
    //     if (buttons.children.item(child).classList.contains("pressed"))
    //     buttons.children.item(child).classList.remove("pressed");
    // }

    // var panel = document.getElementById("add");
    // panel.innerHTML = '';
    // panel.style.display = "none";
}
